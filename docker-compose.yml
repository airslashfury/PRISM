services:
  postgis:
    build:
      context: ./docker
      dockerfile: Dockerfile.postgis
    image: prism-postgis:16-3.4
    container_name: prism-postgis
    environment:
      POSTGRES_DB: ${POSTGRES_DB:-prism}
      POSTGRES_USER: ${POSTGRES_USER:-prism}
      POSTGRES_PASSWORD: ${POSTGRES_PASSWORD:-prism}
    ports:
      - "${POSTGRES_PORT:-5432}:5432"
    volumes:
      - prism_pgdata:/var/lib/postgresql/data
      - ./docker/initdb:/docker-entrypoint-initdb.d:ro
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U ${POSTGRES_USER:-prism}"]
      interval: 10s
      timeout: 5s
      retries: 5

  api:
    build:
      context: .
      dockerfile: docker/Dockerfile.api
    image: prism-api:0.1.0
    container_name: prism-api
    restart: unless-stopped
    environment:
      POSTGRES_HOST: postgis
      POSTGRES_PORT: "5432"
      POSTGRES_DB: ${POSTGRES_DB:-prism}
      POSTGRES_USER: ${POSTGRES_USER:-prism}
      POSTGRES_PASSWORD: ${POSTGRES_PASSWORD:-prism}
      CORS_ORIGINS: "*"
    ports:
      - "8000:8000"
    volumes:
      - ./data/raw/usgs_3dep:/app/data/raw/usgs_3dep:ro
    depends_on:
      postgis:
        condition: service_healthy

  frontend:
    build:
      context: .
      dockerfile: docker/Dockerfile.frontend
    image: prism-frontend:0.1.0
    container_name: prism-frontend
    environment:
      API_PROXY_TARGET: http://api:8000
      NEXT_PUBLIC_API_BASE: /api
    ports:
      - "3000:3000"
    depends_on:
      - api

  pgadmin:
    image: dpage/pgadmin4:latest
    container_name: prism-pgadmin
    profiles: ["tools"]
    environment:
      PGADMIN_DEFAULT_EMAIL: ${PGADMIN_EMAIL:-admin@prism.local}
      PGADMIN_DEFAULT_PASSWORD: ${PGADMIN_PASSWORD:-prism}
      PGADMIN_CONFIG_SERVER_MODE: "False"
    ports:
      - "5050:80"
    depends_on:
      - postgis

# Optional semantic graph store — uncomment only if Phase 2 needs it (default: graph lives in Postgres)
#  neo4j:
#    image: neo4j:5
#    container_name: prism-neo4j
#    environment:
#      NEO4J_AUTH: neo4j/${NEO4J_PASSWORD:-prism}
#    ports:
#      - "7474:7474"
#      - "7687:7687"
#    volumes:
#      - prism_neo4j:/data

volumes:
  prism_pgdata:
#  prism_neo4j:
